# 2.7 上下文组装与生成

> **核心问题**：怎么把检索结果给到模型？
> **目标**：能让模型的输出既"用得上检索结果"又"能被审计追溯"。

---

## 一、检索到好内容 ≠ 好答案

前面几节把"对的内容"检索出来了。但**把它塞给模型的方式，直接决定模型用不用、用得对不对**。同样一批检索结果，组装和 prompt 写法不同，效果天差地别——一个真实警示：

> **没有明确的 grounding 指令，你的 RAG 系统就是"一个带了文档检索步的幻觉机器"。**

本节三大任务：**① 怎么组装上下文（顺序/格式/长度）② 怎么写 prompt（grounding + 引用）③ 怎么抑制幻觉、做到可溯源**。

---

## 二、上下文组装

### 2.1 Chunk 格式化：给每个块编号带元数据

别把检索到的块直接糊成一坨。**结构化**放进 prompt，每块带来源标识，为引用铺路：

```
<sources>
[Source 1] (来自：产品手册 v3, 第 12 页)
<内容...>

[Source 2] (来自：FAQ, 2026-03 更新)
<内容...>
</sources>
```

### 2.2 顺序：应对 Lost-in-the-Middle

模型对**长上下文的头尾注意力强、中间弱**（详见第六节）。所以顺序不是随便排：

> **最相关的放开头，次相关的放结尾，最不重要的塞中间。** 例如 5 个块按相关性 [1,2,3,4,5]，重排成 **[1, 4, 5, 3, 2]**。

这个简单重排在多文档 QA 上能带来 **10–20% 准确率提升**，几乎零成本。

### 2.3 长度：少而精，胜过多而杂

**别把 20 个块全塞进去**。经验：

- **4–8 个精选块（约 2–4k tokens）通常胜过 20+ 个块（10k+ tokens）**。
- 块越多 → 噪声越多、中间越长（越容易被忽略）、token 成本越高。
- 这正是 2.6 重排的意义：**精排到 top 5、只送 3–5 条**。

### 2.4 上下文压缩（可选进阶）

送之前先压缩：**只抽取与 query 相关的句子/事实**，拼成更紧凑的上下文。适合候选多、token 预算紧的场景。属于"锦上添花"，先把 2.2–2.3 做好。

---

## 三、四个核心 RAG Prompt 模式

2026 年被反复验证的四个可复用模式，套上就能让同一套检索栈明显更可靠：

| 模式 | 干什么 | 关键写法 |
|---|---|---|
| **① Grounding 框定** | 强制只用给定上下文 | "**只根据 sources 回答**"，且作为**第一条指令** |
| **② Explicit Citation 显式引用** | 指定精确引用格式与位置 | "行内引用为 `[Source N]`"——别只说"cite your sources"（太模糊） |
| **③ Chunk Formatting 块格式** | 结构化呈现检索结果 | 用 `[Source N]` + 元数据分隔（见 2.1） |
| **④ Negative Handling 兜底** | 检索不到时怎么办 | "**若 sources 无法回答，就回答：'提供的资料未涵盖此问题'**"；冲突时明确指出并都引用 |

> "Cite your sources" 是**模糊建议**，模型每次理解都不同（有时脚注有时行内）。**必须指定确切格式和位置。**

---

## 四、引用与溯源（Citations）

引用是 RAG 相比裸 LLM 的核心优势——**可审计、可追溯、可建立信任**。

- **强制行内引用**：每个论断后标 `[Source N]`，让答案的每句话都能回溯到原文块。
- **指定格式**：行内 `[Source N]` vs 脚注，明确要哪种。
- **覆盖率检查**：可在后处理里检测"有没有句子缺引用"——研究发现**开源 LLM 超 95% 的答案至少有一句无任何出处**，值得防。

### ⚠️ 一个必须知道的坑：带引用也会幻觉

**Citation-based RAG 仍会幻觉**。失败模式：模型**先用自己参数记忆生成了一个论断，再回头到检索文档里找几个"看起来支持"的 token 贴上引用**（post-hoc 事后找证据），而不是真正从证据推导答案。

- 后果：**引用看起来齐全，内容却是编的**。
- 应对：grounding 指令要强（模式①）、评估要查 faithfulness（答案是否真由上下文支撑，见 2.10 RAGAS）、高风险场景加人工/规则校验（第 8 章）。

---

## 五、幻觉抑制：组合拳

单一手段都不够，叠加使用：

1. **强 Grounding 指令**（模式①，放第一条）。
2. **兜底话术**（模式④）："不知道就说不知道"，别硬编。
3. **冲突显式化**：sources 打架时，让模型指出冲突并都引用，而不是自己选一个。
4. **控制上下文质量**：噪声块越少，幻觉越少（回到 2.6 精排、2.7 少而精）。
5. **置信度信号**：可基于检索相似度 + 引用覆盖率算一个置信分，低置信时触发兜底或转人工。
6. **评估兜底**：2.10 的 faithfulness / groundedness 指标持续监控。

> 核心逻辑：**幻觉抑制 = 好检索（前几节）+ 强约束 prompt（本节）+ 可测评估（2.10）** 三者合力，没有银弹。

---

## 六、Lost-in-the-Middle：2026 仍然真实

单独强调，因为它常被低估：

- **现象**：长上下文里，模型对**开头和结尾注意力强、中间弱**。
- **根因**：RoPE（旋转位置编码）的长程衰减，让模型天然偏向序列首尾——**这是 transformer 的结构性问题**。
- **2026 现状**：**即便 1M token 的模型也没有消除它**，位置偏置依然存在。别以为"上下文够长就没事"。

**应对（组合，前面已散落，这里汇总）**：

1. **重要内容放头尾**（[1,4,5,3,2] 重排，+10~20%）。
2. **限制上下文长度**：重排选 top 3–5，其余丢弃——中间少了就没什么可忽略的。
3. **上下文压缩**：抽关键句，缩短中段。
4. **系统指令放底部**：长 prompt 里，**主命令放最后**，别放顶部（否则被"中间效应"稀释）。

---

## 七、一个可用的 RAG Prompt 模板（落地示例）

把本节四个模式拼起来，一个能直接改用的骨架：

```
你是严谨的知识助手。请严格遵守以下规则回答用户问题：

【规则】                                        ← Grounding(模式①)
1. 只根据 <sources> 中的内容回答，不得使用其中没有的信息。
2. 每个论断后用 [Source N] 标注来源（行内引用）。   ← Citation(模式②)
3. 若 <sources> 无法回答，只回答："提供的资料未涵盖此问题。"  ← 兜底(模式④)
4. 若不同来源冲突，明确指出冲突并分别引用。

<sources>                                       ← Chunk 格式(模式③)
[Source 1] (来源: 产品手册v3 · p12)
{{chunk_1}}
[Source 2] (来源: FAQ · 2026-03)
{{chunk_2}}
...(按 [1,4,5,3,2] 顺序排布，重要的放头尾)
</sources>

用户问题：{{query}}                              ← 主命令放最后(应对中间效应)
```

> 这只是骨架。具体措辞、few-shot 示例、输出格式（是否要 JSON）按业务调，但**四个模式 + 头尾顺序 + 主命令置底**这几个结构性要点建议保留。

---

## 八、本节小结：生成阶段决策清单

1. **grounding 放第一条**：没有它就是"带检索的幻觉机"。
2. **少而精**：送 3–5 个精选块（2–4k tokens），别塞 20 个。
3. **头尾放重点**：[1,4,5,3,2] 重排，应对 lost-in-the-middle（+10~20%）。
4. **主命令置底**：长 prompt 里问题/指令放最后。
5. **引用要具体**：指定 `[Source N]` 格式与位置，别说模糊的"cite sources"。
6. **警惕带引用的幻觉**：post-hoc 贴证据是真问题，靠强 grounding + faithfulness 评估防。
7. **兜底话术**：检索不到就明说"资料未涵盖"，冲突就指出。

> 至此，一条从"原始资料→切片→向量→检索→查询→重排→生成"的完整 Naive/Advanced RAG 主链路走通了。后面 2.8 讲更复杂的架构，2.9 讲多模态，2.10 讲怎么让它在生产里稳定跑。

---

## 参考来源

- [RAG Prompt Engineering Guide (2026) — SurePrompts](https://sureprompts.com/blog/rag-prompt-engineering-guide)
- [The 4 Reusable RAG Prompt Patterns (2026) — SurePrompts](https://sureprompts.com/blog/retrieval-augmented-prompting-patterns)
- [Lost-in-the-Middle Is Still Real in 2026 (Even on 1M-Token Models) — dev.to](https://dev.to/gabrielanhaia/lost-in-the-middle-is-still-real-in-2026-even-on-1m-token-models-2ehj)
- [Solving the 'Lost in the Middle' Problem — Maxim AI](https://www.getmaxim.ai/articles/solving-the-lost-in-the-middle-problem-advanced-rag-techniques-for-long-context-llms/)
- [Why Citation-Based RAG Still Hallucinates — yAI](https://yaihq.com/research/citation-based-rag-still-hallucinates)
- [Attribution Techniques for Mitigating Hallucinated Information in RAG: A Survey (arXiv 2601.19927)](https://arxiv.org/html/2601.19927v1)
